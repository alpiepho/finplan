services:
  finplan:
    build: .
    image: finplan
    stdin_open: true
    tty: true
    volumes:
      - ~/.finplan:/root/.finplan
      - ./examples:/examples:ro

  finplan-mcp:
    build: .
    image: finplan
    entrypoint: ["finplan-mcp"]
    stdin_open: true
